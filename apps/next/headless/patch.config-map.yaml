apiVersion: v1
kind: ConfigMap
metadata:
  name: headless-env
data:
  # Strapi
  STRAPI_ADMIN_BACKEND_URL: 'https://headless-next.harrytang.xyz'
  # Database
  DATABASE_NAME: 'harrytang_staging_headless'
  DATABASE_USERNAME: 'harrytang_staging_headless'
  # STORAGE
  STORAGE_ROOT_PATH: 'next'
